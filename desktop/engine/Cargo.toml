[package]
name = "gecko-engine"
version = "1.0.0"
edition = "2021"
description = "Pure-logic helpers for the GeckoCIRCUITS desktop shell: readiness handshake, engine process handling, filename sanitizing"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
